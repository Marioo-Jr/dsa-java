# Notas — 10 Tries

> Preencha depois de terminar o tópico. O objetivo é escrever o padrão com
> suas próprias palavras — se não conseguir, é sinal de que precisa revisar
> antes de seguir para o próximo tópico.

## O padrão em uma frase

(ex: "quando o problema envolve prefixos de strings — busca, autocomplete,
validação incremental — penso em Trie primeiro.")

## Quando eu reconheço esse padrão num problema novo?

-

## Os 3 erros que mais cometi neste tópico

1.
2.
3.

## Sobre a pasta `teoria/`

Neste tópico a pasta `teoria/` fica vazia (sem arquivo). A única lição do
NeetCode roadmap aqui é "Trie (ADV)", e o próprio primeiro exercício —
`exercicios/ImplementTriePrefixTree.java` (Implement Trie Prefix Tree) — já
É a teoria aplicada: implementar a estrutura de dados do zero. Não faz
sentido duplicar isso em um arquivo de teoria separado.

## Checklist de lições (teoria)

- [ ] Trie (ADV) — aplicada diretamente em `exercicios/ImplementTriePrefixTree.java`

## Checklist de exercícios

Ver `PLAYLIST_INDEX.md` na raiz para o link de cada um.

- [ ] Implement Trie Prefix Tree
- [ ] Design Add And Search Words Data Structure
- [ ] Word Search II
